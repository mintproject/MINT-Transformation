name: mintdt
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.7
  - pip
  - gdal
  - scipy
  - pydap
  - numpy
  - dask
  - pip:
      - drepr
      - ujson
      - netCDF4
      - arpeggio
      - rdflib
      - flask
      - flask_cors
      - requests
      - tqdm
      - marshmallow
      - xarray
      - networkx
      - python-dateutil
      - click
      - ccut
